 # ──────────────────────────────────────────────
 # Cloudflare Tunnel Token
 # ──────────────────────────────────────────────
 # 获取方式：
 #   1. 访问 https://one.dash.cloudflare.com  → Zero Trust → Networks → Tunnels
 #   2. 创建一条隧道 → 选择 Cloudflared 方式
 #   3. 复制 Token（形如 eyJhIjoi...）
 #   4. 将 .env.example 复制为 .env，填入 Token
 TUNNEL_TOKEN=xxx

 # ──────────────────────────────────────────────
 # （可选）API Key — 第二层防护
 # ──────────────────────────────────────────────
 # 如果同时启用了 CfAccessValidationMiddleware，
 # 此 Key 可用于绕过 Cloudflare 直接调试：
 #   curl -H "X-API-Key: your-key" ...
 # PDF_SERVICE_API_KEY=
